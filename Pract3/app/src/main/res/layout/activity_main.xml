<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center_horizontal"
    android:padding="24dp"
    android:background="@color/bg_light_blue">

    <TextView
        android:id="@+id/tvGreeting"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="60dp"
        android:layout_marginBottom="40dp"
        android:text="Привіт! Я Рибалко Ігор, ПЗПІ-23-4"
        android:textSize="22sp"
        android:textStyle="bold"
        android:textColor="@color/text_dark"
        android:gravity="center" />

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="10dp"
        app:cardElevation="4dp"
        app:cardCornerRadius="16dp">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp"
            android:background="#A6FFFFFF">

            <Button
                android:id="@+id/btnGoCalculator"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:text="Калькулятор (II рівень)"
                android:textSize="16sp"
                android:backgroundTint="@color/primary_teal"
                android:textColor="@color/white" />

            <Button
                android:id="@+id/btnGoMovies"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:layout_marginTop="16dp"
                android:text="Список фільмів (III рівень)"
                android:textSize="16sp"
                android:backgroundTint="@color/primary_teal"
                android:textColor="@color/white" />

            <Button
                android:id="@+id/btnGoFitness"
                android:layout_width="match_parent"
                android:layout_height="60dp"
                android:layout_marginTop="16dp"
                android:text="Фітнес трекер (IV рівень)"
                android:textSize="16sp"
                android:backgroundTint="@color/primary_teal"
                android:textColor="@color/white" />

        </LinearLayout>

    </androidx.cardview.widget.CardView>

</LinearLayout>
